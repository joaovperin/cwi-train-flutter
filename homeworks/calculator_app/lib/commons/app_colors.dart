import 'package:flutter/widgets.dart';

class AppColors {
  const AppColors._();

  static const Color primary = Color(0xFF00A6FF);
  static const Color primaryDark = Color(0xFF0082CC);
  static const Color primaryLight = Color(0xFF00B8FF);
  static const Color primaryLightest = Color(0xFF00E6FF);
  static const Color primaryDarkest = Color(0xFF0052CC);
  
}
