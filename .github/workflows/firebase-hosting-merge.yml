# Integração contínua na Main - Web (Firebase hosting)
name: Flutter CI - Web Firebase Deploy on MERGE

'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:

      - uses: subosito/flutter-action@v1
        with:
          channel: "stable"

      - uses: actions/checkout@v2

      - name: Download Dependencies
        run: flutter pub get

      - run: cd homeworks/cleandex_poketecture
      - run: flutter build web

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_CLEANDEX_POKETECTURE }}'
          channelId: live
          projectId: cleandex-poketecture
